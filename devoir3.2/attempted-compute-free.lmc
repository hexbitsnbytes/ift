



# self modification of 5XX where XX is the value of TAB initially
# Then becomes TAB+1 each successive iteration
	#BR	selfModifyWrite
loadCounter
	LDA	counter
selfModifyWrite
	STO	TAB

	LDA	selfModifyWrite
	ADD	one
	STO	selfModifyWrite

	BR condition

condition
	LDA	i
	SUB	counter
	BRZ	endLoop
	BRP	count
	BR	endLoop	# if negative

count
	LDA	counter
	ADD	one
	STO	counter
	BR	loadCounter


endLoop
	
	HLT

one	DAT	1
counter	DAT	1
i	DAT	81
TAB	DAT	19
